import type { LearnData } from '../types/learn.uts'
import { request } from '../utils/request.uts'

/**
 * 获取学习路径数据
 */
export async function fetchLearnData(): Promise<LearnData> {
  return request<LearnData>({
    url: '/api/learn/data'
  })
}
